export interface CommentaireResponseDTO {
  id: string;
  dateCreation: string;
  commentaire: string;
  owner: string;
}

/**
 * @typedef {object} CommentaireResponseDTO
 * @property {string} id
 * @property {string} commentaire
 * @property {string} dateCreation
 * @property {string} owner
 */
