export interface CommentaireRequestDTO {
  commentaire: string;
  owner: string;
}

/**
 * @typedef {object} CommentaireRequestDTO
 * @property {string} commentaire.required
 * @property {string} owner
 */
